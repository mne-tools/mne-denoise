SOUND
=====

This example uses the MNE Sample EEG recording and its public forward solution
to show how forward-model information can estimate channel-specific noise.
Known planted corruption provides an artifact-recovery endpoint, while
applying the same fitted operator to the unmodified recording provides a
signal-preservation control.

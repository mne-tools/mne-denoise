iCanClean
=========

This example demonstrates reference-informed CCA cleaning with iCanClean using
a controlled recording in which the artifact and signal of interest are known.

Two reference conditions are compared: one that primarily captures the
artifact and one that also contains signal of interest. The comparison
illustrates why shared variance is not, by itself, evidence that a component
should be removed.

Clarified that ASR cutoff values are specific to the implementation,
calibration rule, and recording regime. The default supports compatibility
comparisons and is not a universal conservative recommendation.

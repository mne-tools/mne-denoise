Fixed ``AdaptiveASR`` PSP/PSW updates on recordings with an incomplete final
segment. Paper-replication runners now use only complete, inclusive AASR update
intervals, and failed ``partial_fit()`` calls leave the previous fitted state
unchanged with a clear minimum-segment error.

Aligned standard ASR spectral shaping and physical-unit handling with the
``clean_rawdata`` reference, matched its ``MinCleanFraction`` calibration
semantics, preserved the fitted statistics-filter state during reconstruction,
and bounded Juggler calibration memory without changing Chebyshev DBSCAN
cluster membership.

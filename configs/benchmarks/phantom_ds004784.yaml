# Arm: phantom_ds004784 — reference-aware cleaning on the iCanClean phantom. Skeleton (P0).
# Independent RE-ANALYSIS of author-created data: reported SEPARATELY from the random-mixture BSS arms.
arm: phantom_ds004784
runner: ground_truth
status: not_started

dataset:
  id: ds004784
  project_relative_path: openneuro/ds004784
  version: pending
  description: iCanClean phantom (known brain + artifact sources)
  trials_are: technical_repeats          # NOT independent human subjects
  required_for: reference_aware_icanclean_claim
  waiver: {allowed_on: verified_technical_or_licensing_barrier, consequence: [track_status_partial, no_independent_ground_truth_icanclean_claim]}

methods_under_test: [icanclean]
comparators:
  required: [none, asr, auto_cca, adaptive_filtering]
  reference_aware: [icanclean, tspca, auto_cca, adaptive_filtering]
tuning: held_out_not_best_on_eval        # original study tuned on the eval data; we use held-out tuning
sweep: {icanclean: {param: r2_threshold, grid: pending}}

metrics:
  primary_target: known_artifact_source_attenuation
  primary_preservation: known_brain_source_preservation
  secondary: [data_quality_score, rank_change, runtime]
preservation_reference: {source: known_phantom_brain_sources, independent_from_method_fit: true}

independence_note: "Independent re-analysis of the publicly released iCanClean phantom (author-created); pair with muscle_ds004505 + own simulation + non-author metrics."
ip_disclosure: {icanclean_patent: "WO 2022/061322", python_impl_authorization: pending}

unit_of_inference: phantom_trial_technical_repeat
storage:
  always_save: [metrics, model_json, qc_arrays]
